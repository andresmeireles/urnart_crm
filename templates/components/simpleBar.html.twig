<div class="bg-{{ barBackground is defined ? barBackground : 'secondary' }}" {{ id is defined ? 'id="'~ id ~'"' : '' }}>
    <div class="sidebar-sticky py-5">
        <ul class="list-unstyled">
            {% for list in listItens %}
                <li class="noline text">
                    <div class="d-flex align-items-center">
                        {{ list.disabled is not defined or list.disabled is same as(false) ? '<a' : '<div' }} href="/{{ metalink }}/{{ pageType }}/{{ list.link }}" class="{{ list.class is defined ? list.class : '' }}">
                            <span class="mx-2 my-2 btn btn-outline-light"><b>{{ list.text }}</b></span>
                        {{ list.disabled is not defined or list.disabled is same as(false) ? '</a>' : '</div>' }}
                    </div>
                </li>
            {% endfor %}

        </ul>
    </div>
</div>
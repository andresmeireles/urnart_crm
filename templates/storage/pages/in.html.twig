<div class="d-none" id="in">
    <div class="col">
        <form>
        <div>
        <div class="row">
            <h2>Entrada de produtos</h2>
        </div>
        <div class="clearfix row">
            <div class="float-left col-md-3"><h4>Produtos</h4></div>
            <div class="float-right col-md-9 form-inline">
                <h4>
                    <div class="input-group">
                        <span class="mx-2">Data</span>
                    <input type="text" target="date"domEL="" name="date" class="form-control date-mask" value="{{"now"|date( "d/m/Y")}}">
                    </div>
                </h4>
            </div>
        </div>
        <div class="my-2">
                <input type="submit" class="btn btn-block btn-success" value="Enviar">
        </div>
        </div>
        <div class="form-inline my-1" cloneField="prod">
            <div class="col-md-12" clone-area>
                <div class="my-2 row" clone-field>
                    <input type="hidden" class="d-none" input-number='true' value="0">
                    <div class="col-md-5">
                    <select name="prod[name]" class="form-control w-100" id="produto">
                        <option value="" selected disabled>Nome do Produto</option>
                        {% for prod in prods %}
                            <option value="{{ prod.getId }}">{{ prod.nome }}</option>
                        {% endfor %}
                    </select>
                    </div>
                    <div class="col-md-5">
                    <input type="text" class="form-control w-100 numbers-only" id="amount" name="prod[amount]" required>
                    </div>
                    <div class="col">
                    <button class="btn btn-primary fas fa-plus" mk-clone></button>
                    <button class="btn btn-danger fas fa-minus" rm-clone></button>
                    </div>
                </div>
            </div>
        </div>
        </form>
    </div>
</div>

{% extends "headerTemplatePrint.html.twig" %}
{% block customStyle %}
    @media print {
        @page  {
            size: landscape;
        }
    }
{% endblock %}
{% block body %}
<div class="container py-3">
    <div class="text-center">
        <p class="display-2">URNAS</p>
        <p class="display-2">PRODUZIDAS</p>
        <p class="display-2">ATÉ {{ data.today | date('d/m/Y') }}</p>
        <p class="display-1 font-weight-bold">TOTAL: {{ data.todayVal }}</p>
        <p class="display-2">EM {{ data.yesterday | date('d/m') }}: {{ data.yesterdayVal }}</p>
    </div>
</div>
{% endblock %}
/**
 * checkUndefined
 * 
 * @param {string} str string para conversão.
 * @returns {String}
 */
module.exports = (str) => {
    if (str === 'undefined') {
        return '';
    }
    
    return str;
};
